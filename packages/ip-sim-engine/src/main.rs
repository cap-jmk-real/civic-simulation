//! Minimal CLI placeholder — extend for batch runs / FFI glue.

fn main() {
    println!("ip-sim-engine {} — use as a library or extend this binary.", env!("CARGO_PKG_VERSION"));
    println!("See packages/ip-sim-engine/SCALING.md for large-N deployment notes.");
}
